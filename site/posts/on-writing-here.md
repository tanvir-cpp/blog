This is the first post on this blog. It exists so the site has something to show on its first load — you can delete it whenever you're ready.

Posts are written in **Markdown**, published from *Telegram* by sending a message to a bot. The first line becomes the title, everything after it becomes the body. Attach a photo and it lands here too.

> A short quote block, for when someone else said it better.

Some code, since that happens:

```js
const hello = () => console.log('world');
```

That's it. Plain text, readable type, nothing else.
